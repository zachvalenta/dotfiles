[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
[user]
	name = zachvalenta
	email = zjv5002@gmail.com
[color]
	ui = auto
[alias]
	a = add -A
	ai = add -p
	b = branch -v
	c = commit -m
	co = checkout
	count = rev-list --count HEAD
	far = remote -v show
	ma = checkout master
	conf = config --global --list
	d = diff --color-words
	ds = diff --staged
	l = log --graph -n 10 --oneline --all --decorate
	nb = checkout -b
	s = status -s
	t = show -s --format=%ci
	u = reset HEAD
[color]
	ui = always
[color "diff"]
	meta = yellow bold
	commit = green bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = "red bold 52"
	newNormal = "green bold"
	newHighlight = "green bold 22"
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[push]
	default = simple
[core]
	pager = diff-so-fancy | less --tabs=4 -RFX
